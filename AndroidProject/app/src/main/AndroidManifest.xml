<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mentalawareness">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chat_Activities.CreateGroupActivity"></activity>
        <activity android:name=".Chat_Activities.ChatPageActivity" />
        <activity android:name=".Chat_Activities.GroupListActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".SourcesActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".CopingMethodsActivity" />
        <activity android:name=".AwarenessActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version"/>
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/maps_api_key"/>
    </application>

</manifest>